# Root Build Outputs

This directory is reserved for generated root-level build outputs such as the local Next.js production build cache and other non-source artifacts.

Do not treat anything in this directory as editable source of truth.
