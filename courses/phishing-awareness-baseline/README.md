# Phishing Awareness Baseline

Baseline course variant derived from `templates/phishing-awareness-template/template.yaml`. Keep `course.yaml` and `template-data.yaml` in Git; treat exported SCORM packages as build output.
