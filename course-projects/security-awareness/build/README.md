Generated SCORM packages and build manifests belong here.

Keep this directory out of source control except for this README. The editable system of record lives in `project.yaml`, `templates/`, `variants/`, and `themes/`.
