# Rebrand Checklist

This note records the Sapio Forge rebrand scope.

## Updated

- landing page, studio, proof center, and admin metadata
- root README and key docs
- starter-repo manifest and visible project/theme author metadata
- validation notes, beta email copy, and SCORM debug log prefix
- default theme logo assets and app icon

## Intentionally unchanged for now

- GitHub repository slug: `deadoscillate/ldt`
- deployment host: current Vercel URL remains in metadata until a branded domain is live
- internal local storage prefixes such as `ldt:*` to avoid wiping beta user state
- route and script paths such as `/studio`, `/validation`, and `scripts/course-project-build.ts`

## Future follow-up if infrastructure changes

- move metadata base URL to a branded domain
- rename repository/package identifiers beyond current safe user-facing updates
- rotate contact and sender addresses if a production support inbox changes
